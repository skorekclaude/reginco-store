<section class="wrap" style="padding:60px 0;max-width:760px"><h1 class="serif" style="font-size:40px;margin-bottom:16px">{{ article.title }}</h1><div style="color:var(--ink2);font-size:13px;margin-bottom:24px">{{ article.published_at | date: '%d.%m.%Y' }}</div><div style="line-height:1.8">{{ article.content }}</div></section>