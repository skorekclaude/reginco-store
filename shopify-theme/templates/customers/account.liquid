<section class="wrap" style="padding:60px 0"><h1 class="serif" style="font-size:38px">{{ 'customer.account.title' | t }}</h1><p style="color:var(--ink2)">{{ customer.email }}</p><a href="{{ routes.account_logout_url }}" class="btn" style="margin-top:16px;display:inline-block">Log out</a></section>